import { REVIEW_STATUS } from "./applicationItems.js";

// Curated procedural-family item bank.
//
// These items exercise fluent execution of a specific procedure at a given
// difficulty (fact recall, algorithm application, symbol selection). The
// student should recognize the procedure without having to model a context
// first.
//
// Conventions mirror `applicationItems.js` but itemId uses the `-proc-` infix,
// e.g. "multiplication-proc-042".
//
// Seeded empty: see `conceptualItems.js` header for the authoring pipeline.

void REVIEW_STATUS;

export const PROCEDURAL_ITEM_BANK = [];
