# areaPerimeter bank design (batch b0821)

1,881 authored items — 36 cells (area / perimeter / compositeFigures /
measureReasoning × 3 families × 3 bands), every cell ≥50. Procedural
auto-approved (651); conceptual (618) + application (612) in `/admin`
review. Generated by:

```bash
node --import ./scripts/lib/registerResolve.js scripts/itemGen/authorAreaPerim.js --tag b0821 [--write]
```

Sources: `scripts/itemGen/areaPerimTemplates.js` (area, perimeter),
`areaPerimTemplates2.js` (compositeFigures, measureReasoning),
`areaPerimStories.js` (application), through the shared `bankAssembler.js`.

## Registers & bands

- Integer answers (numberPad) or string choices. Letter-free forms
  "Area: 5 x 3 = ?" and "Perim: 5 + 3 + 5 + 3 = ?" stay under the
  6-letter verbal threshold and feed the words-off serving path
  (verified 60/60 bank-served at level 1, words off).
- Bands: K-1 dims 2–6 in unit squares/units; 2-3 dims 3–12 in cm;
  4-5 dims to 15 in m, plus missing sides and composite cuts.
- Missing-side data never yields an answer equal to the given side —
  that states the answer in "?"-less phrasings (structureMatch).

## Claims

`display.ap` claims re-derived by `authorAreaPerim.js`: areaOf/perimOf,
missSidePerim (p even and answer positive enforced), joinAreas/cutArea/
missingPart (composite arithmetic), judged areaSaid/perimSaid/joinSaid,
samePerimSaid (equal perimeter AND unequal area both verified), trapNo/
authoredYes/authored/authoredChoice. Misconception traps: add-the-sides
area, two-sides-only perimeter, area-for-perimeter swaps, add-overlapping-
areas, wrong units (cm for area), rotation-changes-area.

## Engine change

`selectVariety` carried the numberBonds-class any-band family fallback —
a level-1 kid asked for procedural could draw band-3 missing-side items.
The family filter now never leaves the band. Parity fixtures regenerated.
`m4Measurement.spec.js`'s swap-distractor probe now passes
`consultBankFamilies: []` (bank items have no display.width/height and
starved the 400-draw sample).

Pattern catalog: `.claude/skills/areaperimeter-mode-ui/SKILL.md`.
