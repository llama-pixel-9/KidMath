// Moved to src/figures/ (pure, no DOM) so the native engine bundle can share
// it with iOS — see nativeEntry.js. Re-exported here for existing importers.
export { areaFigureSpec } from "../figures/areaFigureSpec.js";
